# Habit Tracker Backend

   archivo .env con:
   MONGO_URI=tu_cadena_de_mongodb
   PORT=3001
   no sabia si este archivo tenia que subirlo ya que tiene el usuario y contraseña de mongodb una disculpa si es importante para la actividad.

