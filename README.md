# Habit Tracker Backend

3. Crear archivo .env con:
   MONGO_URI=tu_cadena_de_mongodb
   PORT=3001
   no sabia si este archivo tenia que subirlo ya que tiene el usuario y contraseña de mongodb una disculpa o si no es importante.

